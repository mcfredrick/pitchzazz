# Findings

A consolidated, presentation-ready ledger of every bug found, the
validation/tool that surfaced it, the root cause, and the fix — across
both the Rust engine and the C++/JUCE port. This is the "found a real
problem, understood why, fixed it robustly" evidence for the demo in one
place, rather than scattered across `ARCHITECTURE.md`, `PERFORMANCE_LOG.md`,
and `ROADMAP.md`'s dated checkpoint entries (which still have the full
narrative — this doc is the index/summary, not a replacement).

Append-only, like `PERFORMANCE_LOG.md`: new findings get a new dated
entry, existing entries don't get edited to match later understanding.

## Quick-reference table

| # | Date | Engine | Bug | Found by | Fix |
|---|------|--------|-----|----------|-----|
| 1 | 2023 (original prototype) | Rust | Lock (`Mutex`) taken inside the real-time audio callback | Code review during rebuild | Lock-free `ringbuf` split `Producer`/`Consumer` |
| 2 | 2023 (original prototype) | Rust | Blocking `mpsc::Sender::send()` inside the audio callback | Code review during rebuild | Same lock-free ring buffer replaces the channel |
| 3 | 2023 (original prototype) | Rust | Heap allocation (`data.clone()`) inside the audio callback | Code review during rebuild | Fixed-size stack buffers, no per-callback allocation |
| 4 | 2023 (original prototype) | Rust | Scale-quantization table sized to `notes().len()` silently mismatched `intervals().len()` for some scale/mode combos → wrong "nearest note" | Code review during rebuild | Rewrote to read `Scale::notes()` pitch classes directly, no reconstructed table |
| 5 | 2026-08-17 | Rust | Pitch shifter's oversampling factor (32, unmeasured leftover from the prototype) put most blocks 40-60% over the real-time budget | Live profiling (`docs/PERFORMANCE_LOG.md`) | Dropped to 8 — ~4x reduction in shift-stage cost |
| 6 | 2026-08-17 | Toolchain (Rust) | `rustup`/`libclang` drifted to x86_64-under-Rosetta, breaking `coreaudio-sys`/`bindgen` builds | Build failure + `rustup show` | `rustup default stable-aarch64-apple-darwin` |
| 7 | 2026-08-17 | Toolchain (C++) | Built VST3 was x86_64-only on an arm64 Mac — built fine, failed to load in-process | PluginVal (`Unable to load VST-3 plug-in file`) | Pinned `CMAKE_OSX_ARCHITECTURES=arm64` explicitly in `CMakeLists.txt` — see entry below for root cause |
| 8 | 2026-08-17 | C++ scaffolding | Generated project defaulted the plugin to a MIDI effect (`IS_MIDI_EFFECT TRUE`, `AU_MAIN_TYPE kAudioUnitType_MIDIProcessor`) — wrong regardless of intent: a true JUCE "MIDI effect" type processes MIDI only, no audio I/O at all, which can't host a pitch-*correction* effect either way | Manual review of generated `CMakeLists.txt` | Set `IS_MIDI_EFFECT FALSE`, `AU_MAIN_TYPE kAudioUnitType_Effect` — audio in/out restored. `NEEDS_MIDI_INPUT` left `FALSE` for now; see `docs/ROADMAP.md` Phase 5 for the MIDI-controlled-pitch ("vocoder mode") feature idea this default may have been hinting at |
| 9 | 2026-08-17 | C++ scaffolding | Template hardcoded its own `PLUGIN_CODE`/`PLUGIN_MANUFACTURER_CODE` ("Mrbl"/"Juce") instead of reading ours from `.env` | Manual review of generated `CMakeLists.txt` | Wired `$ENV{PLUGIN_CODE}`/`$ENV{PLUGIN_MANUFACTURER_CODE}` through, matching the `COMPANY_NAME` pattern already in the template |
| 10 | 2026-08-17 | C++ DSP port | JUCE's `dsp::FFT::perform(..., inverse)` auto-normalizes by `1/N`; the Rust engine's FFT libraries (`rustfft`/`realfft`) normalize neither direction | Source audit of both FFT libraries, done proactively before it could ship as a silent bug | Explicit compensating multiply in `PitchDetector::normalizedSquareDifference` and `PitchShifter`'s `accOversamp` constant |
| 11 | 2026-08-17 | C++ real-time wiring | `juce::Thread` destructor asserts (UB) if destroyed while still running — the `AudioProcessor` destructor never stopped the worker thread | Apple's `auval` (`JUCE Assertion failure in juce_Thread.cpp:55`) — not caught by PluginVal | Unified `stopWorker()` helper, called from `prepareToPlay`, `releaseResources`, *and* the destructor, since hosts aren't guaranteed to call `releaseResources()` before destroying the processor. Re-ran `auval -v aufx PIHZ MAFR` after the fix: assertion gone, `AU VALIDATION SUCCEEDED` clean. |
| 12 | 2026-08-17 | Toolchain (Corrosion) | `corrosion_add_cxxbridge`'s `CRATE` argument needs the CMake target name Corrosion actually created, which silently normalizes the Cargo package name's hyphens to underscores (`pitch-core-ffi` → `pitch_core_ffi`) — not documented, and the wrong name fails as a generic `get_target_property() called with non-existent target` error inside `Corrosion.cmake` with no hint it's a naming mismatch | CMake configure error; root-caused by empirically listing `BUILDSYSTEM_TARGETS` after `corrosion_import_crate()` rather than guessing | Used the correct target name (`pitch_core_ffi`); left a comment in `cpp-plugin/CMakeLists.txt` noting this was confirmed empirically, not from documentation |
| 13 | 2026-08-17 | Toolchain (Corrosion + cxx) | The generated cxx bridge header's include path doesn't match plain `cxx-build`'s convention (`<crate-name>/<source-path>.h`) — Corrosion names it after the CMake cxx-bridge target instead (`pitch_core_ffi_cxx/lib.h`), so `#include "pitch-core-ffi/src/lib.rs.h"` (correct for a standalone `cxx-build` project, confirmed by reading the crate's own build output first) fails under Corrosion specifically | Compile error, `fatal error: 'pitch-core-ffi/src/lib.rs.h' file not found`; root-caused by inspecting the actual generated file tree under `build/corrosion_generated/cxxbridge/` rather than assuming cxx's usual convention applied | Used the actual generated path (`pitch_core_ffi_cxx/lib.h`), with a comment explaining the naming difference is Corrosion-specific, not a cxx convention |
| 14 | 2026-08-17 | C++ hot-swap (real-time correctness, not just wiring) | Instant engine swap discarded the outgoing engine's warmed-up internal phase-vocoder state (windowing FIFO, phase accumulator, overlap-add buffer) and started the new engine cold — an audible-scale discontinuity, not a crash or hang | A new automated dropout-detection test (`tests/DSP/HotSwapDropoutTests.cpp`, per `docs/TESTING.md`), self-calibrated against a no-swap baseline rather than an arbitrary threshold: measured 0.340 max sample delta vs. a 0.014 natural baseline (~24x) — no existing test (unit tests, PluginVal, `auval`) would have caught this, since each engine was individually correct in isolation. **Also caught something the automated test didn't**: after the fix below got the metric comfortably passing, a manual listening test in Ableton still heard a quiet click — the automated click-detector threshold and genuine perceptual inaudibility turned out not to be the same bar, confirmed by a human ear, not assumed. | Crossfade in `CorrectorWorker`: both engines process the same input in parallel for a window after a swap, blended, before fully switching over. Iterated three times against real measurements, not by loosening the test: (1) 1-block linear blend → 0.201, still failing; (2) 4-block linear blend → 0.051, passing the automated test, but a listening test still heard a quiet click; (3) 4-block **equal-power** blend (cos/sin gains instead of linear, the standard fix for the loudness-dip a linear amplitude crossfade causes) → user-confirmed "better, but still there." The automated metric actually rose slightly with this change (0.051→0.078, still well under threshold) even as the perceptual result improved — the click-detector metric and perceived smoothness aren't the same thing, worth remembering when trusting either number alone. Root cause of the remaining residual: the two engines' phase accumulators are independently evolving with no shared reference point, which crossfading (blending amplitude) doesn't fully address — a real limitation of swapping between two differently-implemented stateful algorithms, not a bug to hunt down further within the current approach. |
| 15 | 2026-08-17 | Toolchain (C++ build) | A fresh (from-empty-`build/`) CMake configure failed outright with "No CMAKE_C_COMPILER could be found" after this machine's Xcode updated to 26.6, which dropped the legacy `Developer/usr/bin/cc` symlink that Homebrew's `cmake` 3.23.1 (installed mid-2022) depended on for auto-detecting the compiler. Incremental builds on an already-configured `build/` dir kept working, masking the break until a new header file (`PitchzazzLookAndFeel.h`) forced a full regen. | `./scripts/generate_and_open_xcode.sh`'s fresh configure failing; confirmed by manually reproducing with explicit `-DCMAKE_C_COMPILER=.../XcodeDefault.xctoolchain/usr/bin/cc` overrides, which got the top-level configure past it but then failed identically inside JUCE's internal `juceaide` sub-configure (an independent `cmake` invocation that doesn't inherit top-level `-D` flags) — proving per-invocation flags weren't a real fix | `brew upgrade cmake` (3.23.1 → 4.4.2), with explicit user confirmation first since it's a machine-wide tool change, not scoped to this project |
| 16 | 2026-08-17 | Toolchain (C++ build) | `scripts/build.sh`'s own `configure_cmake()` function calls unqualified `cmake`, which resolves via `PATH` to a *third*, separate Intel-Rosetta Homebrew install (`/usr/local/Cellar/cmake/3.27.7`) — different from both the already-fixed `/opt/homebrew/bin/cmake` (4.4.2) and the one `generate_and_open_xcode.sh` already explicitly hardcodes. Same root cause as finding #7 (dual Homebrew installs, `/usr/local/bin` earlier in `PATH`), resurfacing in a second, independent code path that hadn't been fixed the first time around. | `./scripts/build.sh`'s configure step failing with the same "No CMAKE_C_COMPILER could be found" class of error as finding #15, immediately after finding #15's fix had already resolved it for `generate_and_open_xcode.sh` — the mismatch between the two scripts' behavior was the tell | Hardcoded `/opt/homebrew/bin/cmake` in `configure_cmake()`'s macOS branch, mirroring the existing precedent already in `generate_and_open_xcode.sh`, with a comment cross-referencing both this and finding #7 |
| 17 | 2026-08-17 | C++ build correctness (real-time-safety-adjacent) | Building with a stale CMake cache left over from finding #16's investigation (`CMAKE_OSX_ARCHITECTURES=arm64;x86_64`, from `generate_and_open_xcode.sh`'s own default multi-arch flag) silently produced a **universal-binary build request** even though `CMakeLists.txt` was believed to pin `arm64` only — because that pin used `if(NOT CMAKE_OSX_ARCHITECTURES)`, a *soft default* that only applies when the variable is otherwise unset, not a hard override. Harmless before `crates/pitch-core-ffi` existed; once it did, the x86_64 slice's link failed outright (`symbol(s) not found for architecture x86_64` against `libpitch_core_ffi.a`, which Corrosion only ever builds for `aarch64-apple-darwin`) | AU build failure during a routine rebuild after pruning unrelated template scaffolding — not caught by any earlier build in this session because no prior build had both (a) a stale multi-arch cache value and (b) the Rust FFI bridge present at the same time | Changed the `CMakeLists.txt` pin from a soft `if(NOT CMAKE_OSX_ARCHITECTURES)`-guarded default to an unconditional `FORCE`, so no external `-D` flag (from either build script) can silently re-enable a universal-binary build the Rust FFI bridge can't support |
| 18 | 2026-08-17 | C++ DSP correctness (new TD-PSOLA engine) | `PSOLAPitchShifter::placeGrainAt`'s read-position calculation (`std::floor(synthesisMarkPos / periodSamples) * periodSamples`) silently read an entire extra period of **stale, wrong-grain content** whenever floating-point rounding from repeated `+=` accumulation left `synthesisMarkPos` a few ULPs *below* an exact integer multiple of `periodSamples` instead of exactly on it — `std::floor(17.999999997)` gives `17`, not the intended `18`. Only manifested for specific sample-rate/frequency combinations (whichever ones happened to land on the wrong side of the rounding boundary), not all of them — 3 of 4 initially-tested combinations were unaffected, which made it look at first like "expected overlap-add onset smoothing," a real but different phenomenon, rather than a bug. | A new latency probe (`benchmarks/PSOLALatencyProbe.cpp`) built specifically because this engine's latency is pitch-dependent, unlike the phase vocoder's — LatencyProbe.cpp's existing impulse-peak method doesn't work for an algorithm that needs real pitch content to do anything. The probe found `48kHz/220Hz` measuring ~220 samples over the derived worst-case latency while `44.1kHz/220Hz` and both `60Hz` cases matched exactly; increasing the safety margin (tried 2x/3x/4x `maxPeriodSamples`) had *zero* effect on the gap's size, which was the tell that it wasn't a margin problem — confirmed by direct instrumentation (temporary `fprintf` mark-firing trace, compiled and run standalone outside the normal build) rather than continued guessing, after two independent wrong hypotheses (cross-correlation periodicity ambiguity in the probe itself, then a red herring around the test tone's exact zero-crossing at onset) were each tested and ruled out in turn. | Added a `+1e-6` epsilon before the `floor()` call, nudging near-integer-from-below values back onto the correct integer first. Re-verified exact (not just closer) across all 4 tested sample-rate/frequency combinations afterward — the derived `2 * maxPeriodSamples` latency bound needed no padding once the actual bug was fixed. |
| 19 | 2026-08-17 | C++ DSP quality (TD-PSOLA engine) | `placeGrainAt` read source content from a single, floor-quantized analysis "bucket" (one nearest-period-multiple position) rather than the true continuous signal — for a *stationary* test tone this is harmless (every bucket is mathematically identical), but for real, non-stationary voice, consecutive buckets differ slightly, so the source content jumps discretely every time the synthesis position crosses a bucket boundary. Audible as crackle plus a low-frequency beat (the periodic pattern of bucket-boundary crossings itself). | Real-time listening on live vocal audio through the Standalone app, not any unit test — deliberately noted, since three separate automated approaches were tried afterward specifically to catch this and none of them discriminated the fixed version from the deliberately-broken one (full account in `tests/DSP/PSOLAPitchShifterTests.cpp`'s comments: a stationary-sine test can't expose the bug at all; an externally-synthesized reference tone was phase-misaligned and meaningless; a statistical max/median delta-outlier ratio on a tremolo-modulated signal measured *lower* for the broken version than the fixed one on one real run). Same lesson this project already logged once (finding #14): an automated metric passing and genuine perceptual quality are not the same bar. | Cross-fade between the two *nearest* analysis buckets (weighted by proximity) instead of snapping to the closer one alone — the standard PSOLA fix for exactly this artifact class. Costs one more period of lookahead (the farther bucket must also be available), so `getLatencySamples()`'s derivation went from `2 * maxPeriodSamples` to `3 *` — see `docs/PERFORMANCE_LOG.md`'s dated entry for the full latency accounting, including a separate, unrelated `minHz` tightening (60Hz -> 80Hz) done in the same session that partially offset the cost. Shipped on the strength of the DSP reasoning (the discrete content jump is a real, identifiable defect independent of how hard it is to score automatically) with real-audio confirmation from the user still the open verification step, not an automated test. **Superseded by finding #20 — the fix didn't work.** |
| 20 | 2026-08-17 | C++ DSP quality (TD-PSOLA engine, follow-up to #19) | The cross-fade fix from #19 did **not** fix the crackle/beat artifact on real listening — user-reported "still present, possibly worse." The DSP reasoning behind #19's fix (discrete content jumps at bucket boundaries) was directionally real but incomplete: blending two analysis buckets exactly one period apart is only a clean cross-fade if the signal is truly periodic at exactly that spacing, and real voice isn't — `periodSamples` is a fixed per-block (~46ms) estimate, but actual vocal-fold vibration has natural cycle-to-cycle jitter, so the true period drifts out of sync with the fixed estimate over a block. Blending two similar-but-phase-shifted waveforms produces frequency-dependent cancellation (comb filtering) that sweeps as the misalignment changes — plausibly explaining both why it didn't help and why it may have sounded worse. **Not confirmed by measurement** — this is the best available diagnosis, not a verified root cause; no automated way exists in this project to score it. | Real-time listening on live vocal audio, again — the only tool in this whole engine's development that has actually caught this artifact class at all; every automated attempt (finding #19's three, none discriminating) has failed to. A high/low-pass filter was also raised as a possible fix and rejected on reasoning alone, not tested: beating shows up as sidebands around the pitch itself (not a separable low-frequency component), and crackle is a broadband transient — neither has a spectral signature a simple threshold filter would cleanly remove. | Reverted the cross-fade entirely — back to single-bucket `placeGrainAt` and `latencySamples = 2 * maxPeriodSamples` (~25ms, matching finding #19's `minHz` tightening's full gain, none given back this time). Documented the crackle as a known, accepted limitation of this simplified PSOLA design rather than continuing to iterate blind — a real fix needs correlation-based grain alignment before blending, not blending alone, which is bigger scope than this project's timeline has room for. This engine's demonstrated value is the latency comparison against the phase vocoder, not production-grade reconstruction quality — the artifact is disclosed, not hidden. |
| 21 | 2026-08-17 | C++ Standalone (macOS privacy) | Microphone input silently never worked in the Standalone app — no crash, no visible error, the input stream just never delivered samples, blocking any live-mic testing for the entire session up to this point. Root cause: `CMakeLists.txt`'s `juce_add_plugin()` call never set JUCE's `MICROPHONE_PERMISSION_ENABLED` option, so the generated Standalone app's `Info.plist` never got an `NSMicrophoneUsageDescription` key — and modern macOS's privacy/TCC system silently denies microphone access to any app missing that key. | User's own hypothesis prompted the investigation ("is the input mute control unwired?") — checked first and ruled out: this codebase has no custom mute control at all (`grep` for `mute` across `Source/` found none). Confirmed the real cause empirically, not from JUCE's docs alone: `PlistBuddy -c "Print :NSMicrophoneUsageDescription"` against the actual built app showed the key genuinely absent. | Added `MICROPHONE_PERMISSION_ENABLED TRUE` and `MICROPHONE_PERMISSION_TEXT "..."` to the `juce_add_plugin()` call (AU/VST3 don't need this — the host DAW's own Info.plist/entitlements own microphone permission for plugins running inside it). Re-verified via `PlistBuddy` that the key is now present, then confirmed by the user: the mic works. |

## Validation tools used, and what each one actually catches

Worth being explicit about this for the demo — no single tool caught
everything, and that's the point:

- **`cargo test` / Catch2 (`Tests` target)**: unit-level correctness —
  algorithm logic, edge cases, numeric parity between the two engines.
  Catches logic bugs, not host-integration or lifecycle bugs (finding #11
  passed every unit test; the plugin's DSP logic was never the problem).
- **`cargo clippy`**: Rust-side lint/style — not used for anything in this
  table, but part of the standard gate (`CLAUDE.md` rule 5).
- **PluginVal (strictness 5)**: host-level plugin-lifecycle stress
  testing — many sample rates/block sizes, bus configuration, editor
  lifecycle, state save/restore. Caught #7 (architecture mismatch) but
  *not* #11 (the thread-destruction assertion) — PluginVal's own harness
  apparently doesn't exercise the "destroy without calling
  releaseResources" path the way `auval` does.
- **Apple's `auval`**: the actual validator Logic Pro/GarageBand use
  internally, run independently of PluginVal's harness. Caught #11, which
  PluginVal missed — direct evidence that one validator isn't enough and
  cross-checking against a second, independently-implemented one matters.
- **Manual source audit** (reading the actual crate/framework source in
  `~/.cargo/registry/src/` and `~/.juce_cache/juce-src/`, not trusting
  docs): caught #10 before it ever produced a wrong answer, and confirmed
  #1/#2's `ringbuf` fix and this port's `juce::AbstractFifo` choice were
  actually lock-free rather than assuming it from the class name. Doesn't
  catch integration/lifecycle bugs the way a real host validator does —
  #11 was invisible to source audit since `AudioProcessor`'s own
  destructor code was correct in isolation; the bug was in what *wasn't*
  there (no `stopThread()` in the destructor).

## Full narrative

Each finding's complete story — data, hypothesis, fix, re-verification —
lives in dated entries in the doc closest to what changed:

- Findings #1-4: `docs/ARCHITECTURE.md`'s "What the original prototype
  got wrong" and `crates/pitch-core/src/scale.rs`'s module doc.
- Finding #5: `docs/PERFORMANCE_LOG.md`.
- Finding #6: root `CLAUDE.md`'s Build/test/run section.
- Findings #7-11: `docs/ROADMAP.md` Phase 2's dated Day 1/3/4 checkpoint
  entries.
- Findings #12-14: `docs/ROADMAP.md` Phase 3's hot-swap progress entry;
  finding #14's full before/after numbers also in `docs/TESTING.md`'s
  "Hot-swap dropout detection" section.
- Findings #15-17: no separate checkpoint doc — these surfaced during
  repo-hygiene work (pruning unused template scaffolding, then pushing to
  GitHub for the first time), not a roadmap phase, so the table cells
  above are the complete narrative. All three are toolchain-drift bugs in
  the same family as finding #7 — same root cause pattern (dual Homebrew
  installs, or a soft default silently overridden), rediscovered in new
  code paths rather than the same one recurring.
- Finding #18: `docs/ROADMAP.md` Phase 5's TD-PSOLA engine entry has the
  scoping/design story; `docs/PERFORMANCE_LOG.md`'s dated entry has the
  full measured latency numbers (both before and after this fix).
- Finding #19: `docs/PERFORMANCE_LOG.md`'s "TD-PSOLA revisited" dated
  entry has the full latency-accounting story (this fix plus the
  separate `minHz` tightening, net effect on the phase-vocoder
  comparison); `docs/ROADMAP.md` Phase 5's TD-PSOLA entry has the
  scoping/status update.
- Finding #20: `docs/PERFORMANCE_LOG.md`'s "TD-PSOLA cross-fade
  reverted" dated entry has the full diagnosis and the numbers after
  reverting.
- Finding #21: no separate checkpoint doc — resolved inline during a
  documentation pass, the table cell above is the complete narrative.
