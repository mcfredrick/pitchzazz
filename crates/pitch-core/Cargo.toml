[package]
name = "pitch-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Real-time-safe pitch detection, scale quantization, and pitch shifting — no I/O, no threading, pure DSP."

[dependencies]
pitch-detection = "0.3"
pitch_shift = "1.0"
rust-music-theory = "0.2"

[dev-dependencies]
hound = "3"
